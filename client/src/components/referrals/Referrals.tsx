import React from 'react';
import { Container, Typography } from '@mui/material';

const Referrals: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Referrals Component</Typography>
      <Typography>Referral system will be displayed here</Typography>
    </Container>
  );
};

export default Referrals;