{
  "name": "gpt-rewards-website",
  "version": "1.0.0",
  "description": "A GPT (Get-Paid-To) reward website similar to freecash.com",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["gpt", "rewards", "earning", "surveys", "cryptocurrency"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}